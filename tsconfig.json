{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",  // Definindo a raiz do projeto como base
    "paths": {
      "@/*": ["src/*"]  // Alias '@' apontando para o diretório 'src'
    }
  }
}
